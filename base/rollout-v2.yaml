apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: demo
  namespace: demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: demo
  strategy:
    canary:
      canaryService: demo-canary
      stableService: demo
      trafficRouting:
        istio:
          virtualService:
            name: demo-vs
            routes:
            - default
      steps:
      - setWeight: 10
      - pause: { duration: 30 }
      - setWeight: 30
      - pause: { duration: 30 }
      - setWeight: 50
      - pause: { duration: 30 }
      - setWeight: 100
  template:
    metadata:
      labels:
        app: demo
        version: v3
    spec:
      containers:
      - name: demo
        image: ghcr.io/fullstackjam/demo:v3
        env:
        - name: VERSION
          value: "v3"
        ports:
        - containerPort: 8080
