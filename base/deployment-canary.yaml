apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-canary
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
      version: canary
  template:
    metadata:
      labels:
        app: demo
        version: canary
    spec:
      containers:
      - name: demo
        image: ghcr.io/fullstackjam/miniproject:86d8f08a373fb71aee63d2e75e4983982d1488b9
        env:
        - name: VERSION
          value: "canary"
        ports:
        - containerPort: 8080
