apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-canary
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
      version: canary
  template:
    metadata:
      labels:
        app: demo
        version: canary
    spec:
      containers:
      - name: demo
        image: ghcr.io/fullstackjam/miniproject:398d68ebd1cbdbee95cb18437aa330b43368dbfb
        env:
        - name: VERSION
          value: "canary"
        ports:
        - containerPort: 8080
